export function createTokenPair(userId: number): { access_token: string; refresh_token: string } {
    // Логіка для створення токенів
    return { access_token: 'access_token', refresh_token: 'refresh_token' };
} 